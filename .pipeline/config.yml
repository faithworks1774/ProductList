general:
  buildTool: mta

stages:
  Build:
    mtaBuild: true

  UploadToTransportManagement:
    uploadToTransportManagement: true

  Release:
    cloudFoundryDeploy: true

steps:
  cloudFoundryDeploy:
    cfCredentialsId: btplogin
    cfApiEndpoint: https://api.cf.us10-001.hana.ondemand.com
    cfOrg: 3d1a9f0ftrial
    cfSpace: dev
  uploadToTransportManagement:
    nodeName: "TEST"
    credentialsId: "tms"
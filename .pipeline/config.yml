general:
  buildTool: "mta"

stages:
  Build:
    mtaBuild: true

  Deploy:
    cloudFoundryDeploy: true

steps:
  cloudFoundryDeploy:
    cfCredentialsId: "cf-service-key"
    cfApiEndpoint: "https://api.cf.us10-001.hana.ondemand.com"
    cfOrg: "3d1a9f0ftrial"
    cfSpace: "dev"